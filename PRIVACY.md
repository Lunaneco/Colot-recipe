# プライバシー

## アプリが扱うデータ

カラーレシピは、保存色、配合、画面設定、おえかき、塗り絵、読み込んだ線画をブラウザのIndexedDBとLocalStorageへ保存します。アカウント、Cookieによる追跡、広告、アクセス解析、外部API、クラウド同期は使用しません。

チュートリアル動画、字幕、画像、フォント、スクリプトは同一オリジンから読み込みます。アプリ自身は制作データをサーバーへ送信しません。

## 読み込んだ画像

線画はPNG・JPEG・WebP、8MB以下、最大8192px・3200万画素に制限します。読み込み後は固定サイズのPNGへ再描画するため、元ファイルに含まれるEXIF、GPS、カメラ情報などは保存しません。元画像ファイルそのものは保持しません。

## 書き出しと復元

JSONやPNGの保存は、利用者がブラウザのダウンロード操作を行ったときだけ実行します。書き出したファイルの保管・共有・削除は利用者の管理です。全データ復元は外部URLや未対応画像を拒否し、検証が完了するまでブラウザ保存領域を書き換えません。

## 削除

ブラウザのサイトデータ削除機能で、IndexedDBとLocalStorageを削除できます。書き出したJSONやPNGは、端末のファイル管理機能から別途削除してください。

## ホスティング

公開先のホスティング事業者やGitHubは、配信のためにIPアドレス、時刻、User-Agentなどの標準的なアクセスログを独自の方針で処理する場合があります。これはカラーレシピのアプリ内保存とは別です。公開時は採用したホスティング事業者のプライバシーポリシーを確認してください。
