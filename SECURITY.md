# セキュリティーポリシー

## 対応対象

公開後は、`main` ブランチの最新版だけをセキュリティー修正の対象とします。過去のコミットや第三者が改変した配布物は対象外です。

## 脆弱性の報告

公開リポジトリでGitHubのPrivate vulnerability reportingを有効にし、報告はその非公開フォームから受け付けます。有効化前は、脆弱性の詳細、個人データ、バックアップファイル、画像、アクセストークンを公開Issueへ投稿しないでください。

報告には、影響する画面、再現手順、想定される影響、確認したブラウザを含めてください。実在利用者のデータへアクセスする検証、サービス妨害、なりすまし、公開前の情報開示は行わないでください。

## 実装上の方針

- アプリのデータはブラウザ内へ保存し、解析や外部API送信を行いません。
- 画像は容量・形式・寸法を検証し、PNGへ再描画してメタデータを除去します。
- バックアップは全件を検証してから書き込み、外部URLや危険なオブジェクトキーを拒否します。
- `index.html` のCSPで、同一サイト以外のスクリプト、接続、画像、動画、フォーム送信、オブジェクトを拒否します。
- GitHub Pagesでは任意のレスポンスヘッダーを追加できません。クリックジャッキング防止の `frame-ancestors`、Permissions Policy、COOP/CORPなどが必要な運用では、専用ドメインとヘッダーを設定できるホスティングを使用します。
- GitHub Pagesのプロジェクトサイトは `lunaneco.github.io` のオリジンを共有します。保存領域を他のプロジェクトサイトから厳密に分離する必要がある場合は、このアプリ専用のカスタムドメインを使用します。
- 依存関係はlockfileで固定し、CIの外部ActionもコミットSHAで固定します。

## 秘密情報

このプロジェクトはクライアント専用で、実行時の秘密情報を必要としません。秘密値を `VITE_*` へ設定すると配信JavaScriptへ含まれるため、使用しないでください。ホスティング用ID、音声参照、モデル、キャッシュ、個人パス、`.env` は公開対象外です。
